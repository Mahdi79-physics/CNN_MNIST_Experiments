# 🖤 MNIST Neural Network Experiments

This repository contains experiments on the **MNIST dataset** using different neural network architectures in TensorFlow.  
It includes comparisons of **CNNs with and without batch normalization** and **MLPs with and without dropout**.

---

## ✨ Features

### CNN Experiments
- 🧠 2 Convolutional layers + Flatten + Dense  
- ⚡ Compare **with and without batch normalization**  
- 📊 Visualize **training and validation accuracy and loss**

### MLP Experiments
- 🖥️ Fully connected networks (Dense layers)  
- ⚡ Compare **with and without dropout**  
- 📊 Visualize **training and validation accuracy and loss**

---

## 📝 Requirements

- Python 3.8+  
- TensorFlow  
- NumPy  
- Matplotlib  

Install dependencies:

```bash
pip install -r requirements.txt
